bind r source ~/.screenrc

term screen-256color
defscrollback 10000

bind s split
bind S split -v

bind j focus down
bind k focus up
bind h focus left
bind l focus right

bind = resize =
bind + resize +1
bind - resize -1
